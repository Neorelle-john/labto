<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/users" method="POST">
        <label>NAME</label>
        <input type="text" name="name" value="<%= formData?.name || '' %>">
        <% if (errors?.name) { %>
        <p class="error"><%= errors.name.message %></p>
        <% }  %>
          <label>AGE</label>
        <input type="text" name="age" value="<%= formData?.age || '' %>">
        <% if (errors?.age) { %>
        <p class="error"><%= errors.age.message %></p>
        <% }  %>

        <select name="course">
            <option value="">---</option>
            <option value="BSIT" <%= formData?.course === "BSIT" ? "selected" : "" %>>BSIT</option>
            <option value="BSCE" <%= formData?.course === "BSCE" ? "selected" : "" %>>BSCE</option>
            <option value="BSME" <%= formData?.course === "BSME" ? "selected" : "" %>>BSME</option>
        </select>

        <button type="submit">SAVE</button>
    </form>
</body>
</html>